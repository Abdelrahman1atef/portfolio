[build]
  command = "npm run build"
  publish = ".next"

[functions]
  directory = "server/dist"
  node_bundle = true

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/index/api/:splat"
  status = 200
